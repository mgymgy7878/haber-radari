# v0.6.6 — Classification / Publish-Gate Fix

**Date:** 2026-06-28  
**Status:** ✅ PASS

---

## BLOCKER özeti

| Sorun | Kök neden | Düzeltme |
|-------|-----------|----------|
| F1 `kazandı` → `Afet` + kritik ana akış | Substring `kaza` in `kazandı`; `büyük` in `Büyük Britanyalı` | Token/phrase matcher + spor override |
| Balıkesir deprem → `Çevre / Halk Sağlığı` | Substring `balık` in `Balıkesir` | Token tabanlı çevre eşleşmesi; deprem önceliği |

---

## Değişen dosyalar

| Dosya | Rol |
|-------|-----|
| `turkish-text-match.ts` | Token/phrase eşleştirme, spor/afet/kaza kuralları |
| `feed-category.ts` | Öncelikli kategori çözümlemesi |
| `publish-gate.ts` | Spor önce; kritik afet şiddeti token tabanlı |
| `smart-feed.ts` | `resolveFeedCategory` kullanımı |
| `turkish-text-match.test.ts` | 10 yeni test |

---

## Doğrulama (API runtime post-fix)

```
published=2
- İzmir itfaiye kazası → Afet | DISASTER_ALERT (gerçek kaza)
- Balıkesir deprem → Afet / Deprem | DISASTER_ALERT

Formula 1 → latestRssPreview only (LATEST_RSS), ana akışta yok
```

Evidence JSON: `v0.6.6-api-runtime-post-fix.json`

---

## Device screenshots (0.6.6-debug • 66)

| Dosya | Kanıt |
|-------|-------|
| `v0.6.6-earthquake-afet.png` | Deprem kartı `Afet / Deprem` |
| `v0.6.6-formula-not-afet.png` | F1 Latest RSS'te yalnızca `RSS` chip — `Afet` yok |
| `v0.6.6-feed-trust-card.png` | Trust UX korunuyor |
| `v0.6.6-detail-why-shown.png` | Neden gösteriliyor? |
| `v0.6.6-detail-sources.png` | Kaynak listesi |

---

## Test sonuçları

| Suite | Sonuç |
|-------|-------|
| API vitest | **73/73 PASS** |
| Android testDebugUnitTest | **96/96 PASS** |

---

## Güvenlik

| Alan | Değer |
|------|-------|
| `realExternalCallExecuted` | **false** |
| `productionApiKeyUsed` | **false** |
| `operatorApprovalUsed` | **false** |

PR: https://github.com/mgymgy7878/haber-radari/pull/21
