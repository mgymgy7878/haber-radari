# v0.6.4 — Provider Pilot Runbook + Cost/Budget Dashboard

**Durum:** PASS (read-only; gerçek LLM çağrısı yok)

## Özet

Operatör runbook, read-only status endpoint, CLI evidence script ve rollback prosedürü eklendi. Pre-PR manuel doğrulama tamamlandı.

## Değişen dosyalar

| Dosya | Açıklama |
|-------|----------|
| `apps/api/src/smart-digest/status.ts` | Status builder + gate evaluation |
| `apps/api/src/routes/smart-digest-status.ts` | GET handler |
| `apps/api/src/routes/smart-digest-status.test.ts` | 10 test |
| `apps/api/scripts/smart-digest-status.ts` | CLI evidence script |
| `apps/api/src/server.ts` | Route registration |
| `apps/api/package.json` | `smart-digest:status` script |
| `docs/ops/v0.6.4-real-provider-pilot-runbook.md` | Operatör runbook |

## Status endpoint contract

```
GET /api/v1/smart-digest/status
```

Read-only. API key değeri **asla dönmez**; yalnızca `apiKeyConfigured: boolean`.

## Pre-PR manuel doğrulama (2026-06-28)

### HTTP endpoint

Kaynak: `evidence/v0.6.4-smart-digest-status-http.json`

| Alan | Değer |
|------|-------|
| `externalEnabled` | **false** |
| `operatorApproved` | **false** |
| `safety.realExternalCallAllowedNow` | **false** |
| `safety.reasonIfBlocked` | `DIGEST_DISABLED` |
| `safety.apiKeyExposed` | **false** |
| API key değeri response'ta | **yok** |
| prompt/response içeriği | **yok** |
| budget counters | valid JSON |

Server env: `LLM_DIGEST_ENABLED=0`, `LLM_DIGEST_EXTERNAL_ENABLED=0`, `OPERATOR_APPROVED_REAL_LLM_DIGEST=0`

### CLI

Kaynak: `evidence/v0.6.4-smart-digest-status.json`

| Alan | Değer |
|------|-------|
| `realExternalCallAllowedNow` | **false** |
| `apiKeyExposed` | **false** |
| Secret basıldı mı | **hayır** |

### Secret taraması

`git diff` + v0.6.4 kaynak dosyaları: gerçek key yok.

## Test sonuçları

| Suite | Sonuç |
|-------|-------|
| API vitest | **63/63 PASS** |
| API build | **PASS** |
| Android testDebugUnitTest | **PASS** |

## Evidence

- `evidence/v0.6.4-smart-digest-status-http.json` (manuel HTTP)
- `evidence/v0.6.4-smart-digest-status.json` (CLI)
- `evidence/v0.6.4-provider-pilot-runbook.md`
- `evidence/v0.6.4.md`

---

## FINAL SUMMARY

| Alan | Değer |
|------|-------|
| Local manuel HTTP doğrulandı mı? | **Evet** |
| CLI doğrulandı mı? | **Evet** |
| Secret sızıntısı var mı? | **Hayır** |
| API key değeri döndü mü? | **Hayır** |
| `realExternalCallExecuted` | **false** |
| `productionApiKeyUsed` | **false** |
| `operatorApprovalUsed` | **false** |

Gerçek provider pilotu için hâlâ ayrı açık onay gerekir:

> *"v0.6.3 gerçek provider pilotu için 1 çağrıya onay veriyorum"*
