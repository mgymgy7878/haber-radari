# v0.6.2 — Android Smart Digest UI

**Durum:** PASS (backend contract render-only; LLM/API key Android'de yok)

## FINAL SUMMARY

### Değişen dosyalar

| Dosya | Değişiklik |
|-------|------------|
| `data/model/SmartDigest.kt` | Domain model + status/confidence enum |
| `data/network/dto/AiCuratedFeedDto.kt` | `SmartDigestDto`, item alanı |
| `data/model/AiCuratedNewsItem.kt` | `smartDigest?` |
| `data/network/mapper/SmartDigestMapper.kt` | Null-safe DTO → domain |
| `domain/repository/RemoteAiCuratedFeedRepository.kt` | Mapping |
| `ui/feed/SmartDigestUiLogic.kt` | Göster/gizle kuralları |
| `ui/feed/SmartDigestSection.kt` | Compose UI (kart + detay) |
| `ui/feed/FeedScreen.kt` | Ana akış kartında digest bölümü |
| `ui/feed/AiCuratedDetailScreen.kt` | Detayda geniş digest |
| `app/build.gradle.kts` | `0.6.2-debug`, versionCode 62 |
| Test dosyaları | UiLogic, Mapper, Repository |

### DTO / domain mapping

- Gson bilinmeyen alanları ignore eder.
- `SmartDigestMapper.fromDto()` geçersiz status'ta `null` döner — crash yok.
- `AiCuratedNewsItem.smartDigest` optional; null → mevcut kart aynen render.

### SmartDigest UI davranışı

**Kart (ana akış):**
- Rozet: `AI özeti` + `Metadata tabanlıdır`
- Summary, max 3 keyPoint, opsiyonel "Neden önemli?", güven etiketi
- Debug: küçük `digest: STATUS` notu

**Detay:**
- Başlık bloğu + metadata uyarısı (orijinal haberin yerine geçmez)
- Kaynak listesi korunur

### Status gösterim matrisi

| Status | Release | Debug |
|--------|---------|-------|
| MOCKED / CACHED / GENERATED + summary | Göster | Göster |
| DISABLED | Gizle | "Digest kapalı" |
| FAILED | Gizle (panik yok) | "Digest üretilemedi" |
| null | Gizle | Gizle |

### Latest RSS / watchlist sızıntısı

- **Yok.** `WatchlistPreviewItem` smartDigest taşımaz.
- `LatestRssItemCard` / watchlist modal digest render etmez.
- RSS başlığı altında mevcut "AI doğrulaması değildir" notu korunur.

### Test sonuçları

| Suite | Sonuç |
|-------|-------|
| Android `testDebugUnitTest` | **PASS** |
| API `vitest run src` | **39/39 PASS** |
| API `pnpm build` | **PASS** |

### Screenshot path'leri

- `evidence/v0.6.2-digest-feed-card.png` — AI özeti + metadata rozeti (MOCKED)
- `evidence/v0.6.2-digest-detail.png` — detay ekranı geniş digest + uyarı metni
- `evidence/v0.6.2-latest-rss-no-digest.png` — Son Haberler RSS kartları digest içermiyor
- `evidence/v0.6.2-digest-disabled-safe.png` — `LLM_DIGEST_ENABLED=0`, debug "Digest kapalı"
- `evidence/v0.6.2-logcat.txt`
- Watchlist modal: `WatchlistModal` composable digest render etmez (kod doğrulaması + unit test); modal yalnızca ana akış boşken açılır

### Android'da API key / LLM çağrısı

**Yok.** Yalnızca backend JSON parse + Compose render.

### v0.6.2 önerisi

```text
v0.6.2 Android Smart Digest UI: PASS
```

Sonraki adım: **v0.6.3 Controlled Real Provider Pilot**
