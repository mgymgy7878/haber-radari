# v0.6.6 — Android Trust & Source Transparency UX

**Durum:** ✅ PASS (UX + classification fix; gerçek LLM çağrısı yok)

**Date:** 2026-06-28

---

## Özet

Feed kartı ve detay ekranında kaynak güveni, publish nedeni, digest durumu ve orijinal linkler netleştirildi. Cihaz kanıtı **doğru build (0.6.6-debug / 66)** ile alındı.

---

## Cihaz doğrulama

| Alan | Before (BLOCKER) | After |
|------|------------------|-------|
| versionName | `0.6.2-debug` | **`0.6.6-debug`** |
| versionCode | `62` | **`66`** |
| UI build marker | `debug 0.6.2-debug • 62 • b562c43` | **`debug 0.6.6-debug • 66 • 4f76d90`** |

### Cihaz bilgileri

| Alan | Değer |
|------|-------|
| Model | M2101K6G (Xiaomi Redmi Note 10 Pro / sweet) |
| Android | 13 |
| ADB serial | `120d06e1` |
| Package | `com.haberradari` |

### Kurulum

```
adb uninstall com.haberradari
./gradlew clean installDebug
adb reverse tcp:3001 tcp:3001
```

Backend güvenli mod:

```
LLM_DIGEST_ENABLED=0
LLM_DIGEST_PROVIDER=mock
LLM_DIGEST_EXTERNAL_ENABLED=0
OPERATOR_APPROVED_REAL_LLM_DIGEST=0
PORT=3001
```

Runtime API post-fix (`bypassCache=1`): `publishedCount=2` (gerçek kaza + deprem; F1 yalnızca latestRss)  
Evidence: **`evidence/v0.6.6-api-runtime-post-fix.json`** (final PASS)

> **Evidence hygiene:** `v0.6.6-api-runtime-pre-fix.json` yalnızca **pre-fix blocker evidence** — F1 `Afet` false-positive içerir; final PASS kanıtı olarak kullanılmaz. Post-fix screenshot’lar (~21:30+) final PASS evidence’dır.

---

## Screenshot evidence (final PASS — post-fix, ~21:30+)

| Dosya | Kanıt |
|-------|-------|
| `v0.6.6-build-marker.png` | Header altında `debug 0.6.6-debug • 66 • 4f76d90` |
| `v0.6.6-feed-trust-card.png` | Trust UX + post-fix kategoriler (İzmir kaza + Balıkesir deprem) |
| `v0.6.6-earthquake-afet.png` | Deprem kartı **`Afet / Deprem`** |
| `v0.6.6-formula-not-afet.png` | F1 Latest RSS — yalnızca `RSS` chip, **`Afet` yok** |
| `v0.6.6-detail-why-shown.png` | `Neden gösteriliyor?` bölümü |
| `v0.6.6-detail-sources.png` | `Kaynak haberler` + `Orijinal habere git` |
| `v0.6.6-preview-no-digest.png` | Latest RSS digest göstermiyor |
| `v0.6.6-classification-fix.md` | Sınıflandırma düzeltme özeti |

**Pre-fix blocker only (PASS değil):** Erken oturumda F1’nin ana akışta `Afet` göründüğü ekranlar ve `v0.6.6-api-runtime-pre-fix.json`. Final merge kanıtı olarak kullanılmaz.

### Kontrol listesi

- [x] Build marker görüldü (`0.6.6-debug • 66`)
- [x] PUBLISH_MAIN vardı (2 olay)
- [x] Feed trust card görüldü
- [x] Detail why shown görüldü
- [x] Detail sources görüldü
- [x] Latest RSS digest göstermiyor
- [x] Digest chip feed/detayda `AI özeti: kapalı` (LLM_DIGEST_ENABLED=0)

---

## Değişen dosyalar (kod)

| Dosya | Değişiklik |
|-------|------------|
| `TrustTransparencyUiLogic.kt` | Digest chip, why-shown, preview kuralları |
| `TrustTransparencyComponents.kt` | `DigestStatusChip`, `TrustEvidenceRow`, `WhyShownSection`, `SourceListSection` |
| `SmartDigestSection.kt` | Status chip + FAILED kart düşürmez |
| `CuratedSourceLabels.kt` | çoklu kaynak / tek kaynak etiketleri |
| `FeedScreen.kt` | Feed kart trust satırı |
| `AiCuratedDetailScreen.kt` | Neden gösteriliyor + kaynak listesi |
| `TrustTransparencyUiLogicTest.kt` | Yeni unit testler |
| `app/build.gradle.kts` | 0.6.6-debug / versionCode 66 |

---

## Test sonuçları

| Suite | Sonuç |
|-------|-------|
| Android testDebugUnitTest | **96/96 PASS** |
| API vitest | **73/73 PASS** |

---

## Güvenlik

| Alan | Değer |
|------|-------|
| `realExternalCallExecuted` | **false** |
| `productionApiKeyUsed` | **false** |
| `operatorApprovalUsed` | **false** |

Secret/env taraması: gerçek key/token yok (yalnızca docs/placeholder eşleşmeleri).

Gerçek provider pilotu ayrı operatör onayı gerektirir.

---

## PR

https://github.com/mgymgy7878/haber-radari/pull/21
